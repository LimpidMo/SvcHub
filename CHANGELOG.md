# SvcHub 更新日志

- 版本号说明：主版本号（大更新）+次版本号（新功能/特性）+ 修订号（bug修复/优化）

## [Unreleased]

## [1.1.0] - 2026-09-16

### Added
- 定时启停服务，按每日时间窗口服务全停跳过巡检，过时间窗后再恢复start标签的服务执行巡检
  - 停止时间 / 启动时间：选好自动保存。这段时间内即为停止窗口，支持跨夜窗口
- Wi-Fi 启停服务：打开开关后按所选分组启停，关闭开关则恢复普通保活逻辑，两组的选择的服务互斥，防止冲突
  - 连接组：连上名单 Wi-Fi 时运行，离开即停止
  - 断开组：离开名单 Wi-Fi 时运行，连上即停止
- **优先级**：定时停止窗 > Wi-Fi 策略 > 普通巡检


### Changed
- 保活间隔下限由 5 秒调整为 10 秒,防止短时间内高频调用
- 端口标签：仅 1-65535 显示 `port:` 前缀和`打开` 按钮，其他文本直显可做为描述标签
- 日志弹窗可自动刷新到底部加载新内容，添加清空操作按钮

### Fixed
- 优化多服务项时的页面加载速度，
- 优化删除服务项逻辑，若运行中退出后再删除，防止进程残留

## [1.0.1] - 2026-09-12

### Fixed
- 服务停止逻辑优化：先 SIGTERM 优雅终止，仍存活则 SIGKILL 强杀，防止进程残留
- 日志轮转失效：统一 1MB 上限，超限保留最近 1/4 上下文
- 首次巡检启动逻辑与 su 路径判断：兼容更多设备和 root 管理器，防止启动检查失效导致无法执行首次巡检

## [1.0.0] - 2026-09-02

### Added
- 双类型服务：Termux 服务（Termux 用户环境）与二进制服务（root 环境）
- 开机自启 + 保活：巡检间隔可配（5~86400 秒，默认 60 秒），服务被杀自动拉起
- WebUI 状态页：实时运行状态，支持启动 / 停止 / 重启 / 查看日志，带端口服务页可跳转浏览器打开
- WebUI 配置页：添加 / 编辑 / 删除 / 拖拽排序服务，开机命令支持测试模式运行
- 调试日志：可查看服务运行日志与模块调试记录
- 配置持久化：`config.json` 存储，升级模块时自动保留
- LICENSE 文件
